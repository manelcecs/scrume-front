export interface BurnUpDisplay {
    date: string;
    points: number;
    totalHistoryTask: number;
}

export interface BurnDownDisplay {
    date: string;
    points: number;
    totalDates: number;
}